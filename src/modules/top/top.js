'use strict';

const React = require('react');
const {PropTypes} = React;

const Top = (top) => // eslint-disable-line no-unused-vars
  <div>
  Hello flex examples!
  </div>
;

Top.propTypes = {
  top: PropTypes.shape({})
};

module.exports = Top;
